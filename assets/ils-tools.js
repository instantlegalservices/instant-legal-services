window.ILS_TOOLS = (() => {
  const esc = v => window.ILS?.esc ? ILS.esc(v) : String(v ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
  const workspace = () => document.getElementById('toolWorkspace');
  const toolCopy = {
    'legal-deadline-calculator': ['Legal Deadline Calculator','Add or subtract calendar days from an event date. This is a date-planning aid, not a statutory limitation determination.'],
    'interest-calculator': ['Interest Calculator','Simple-interest calculation using principal, annual rate and time.'],
    'case-timeline': ['Case Timeline','Add dated events and generate a chronological case timeline.'],
    'case-checklist': ['Case Checklist','Create a practical document and preparation checklist.'],
    'cheque-bounce-timeline': ['Cheque Bounce Timeline','Organise the key Section 138 NI Act dates. The result is an indicative planning timeline and must be verified against the facts and applicable law.'],
    'limitation-calculator': ['Limitation Calculator','Apply a user-supplied limitation period to a start date. The tool does not determine the legally applicable limitation period or exclusions.'],
    'court-fee-calculator': ['Court Fee Estimator','Estimate a fee using a user-supplied rate/fixed amount. Court fees vary by court, state and proceeding.'],
    'stamp-duty-calculator': ['Stamp Duty Estimator','Estimate stamp duty using a user-supplied rate/fixed amount. State-specific stamp laws and transaction details must be verified.'],
    'maintenance-estimator': ['Maintenance Estimator','A financial-planning aid based on user inputs. It does not predict or recommend a court-ordered maintenance amount.'],
    'legal-problem-diagnostic': ['Legal Problem Diagnostic','Structured intake to identify possible legal routes for professional review; it is not a legal opinion.'],
    'case-preparation-tool': ['Case Preparation Tool','Organise facts, chronology, documents and questions for professional review.'],
    'property-document-checklist': ['Property Document Checklist','Organise common property documents before professional due diligence. Requirements vary by property, transaction and jurisdiction.']
  };
  const sources = {
    'cheque-bounce-timeline': '<a href="https://www.indiacode.nic.in/" target="_blank" rel="noopener">India Code — Negotiable Instruments Act, 1881</a> (Sections 138 and 142).',
    'limitation-calculator': '<a href="https://www.indiacode.nic.in/" target="_blank" rel="noopener">India Code — Limitation Act, 1963</a>.',
    'court-fee-calculator': '<a href="https://www.indiacode.nic.in/" target="_blank" rel="noopener">India Code — Court-Fees Act, 1870</a>; state/court rules may apply.',
    'maintenance-estimator': '<a href="https://www.indiacode.nic.in/" target="_blank" rel="noopener">India Code — Hindu Marriage Act, 1955</a> (Sections 24–25) and other applicable laws.',
    'stamp-duty-calculator': '<a href="https://www.indiacode.nic.in/" target="_blank" rel="noopener">India Code</a>; applicable stamp law and state amendments/rules must be verified.'
  };

  const serviceMap = {
    'legal-deadline-calculator':'legal-consultation',
    'interest-calculator':'legal-consultation',
    'case-timeline':'case-preparation-pack',
    'case-checklist':'case-preparation-pack',
    'cheque-bounce-timeline':'legal-notice-draft',
    'limitation-calculator':'legal-research-report',
    'court-fee-calculator':'legal-consultation',
    'stamp-duty-calculator':'property-due-diligence',
    'maintenance-estimator':'legal-consultation',
    'legal-problem-diagnostic':'legal-consultation',
    'case-preparation-tool':'case-preparation-pack',
    'property-document-checklist':'property-due-diligence'
  };
  async function createOrder(slug, details, toolRunId=null){
    const sb=window.ILS?.ready?.();
    if(!sb){ status('Service connection unavailable.'); return; }
    const {data:{user}}=await sb.auth.getUser();
    if(!user){
      status('Please sign in to start this paid service.');
      setTimeout(()=>{ window.location.href='portal.html#login'; },700);
      return;
    }
    const service=serviceMap[slug];
    if(!service){status('Paid service is not configured yet.');return;}
    const {data,error}=await sb.rpc('ils_create_service_order',{p_service_slug:service,p_details:details||{},p_tool_run_id:toolRunId});
    if(error||!data?.ok){status(error?.message||'Unable to create the service order.');return;}
    const box=document.getElementById('toolStatus');
    if(box){box.className='status ok';box.innerHTML=`Order <strong>${esc(data.order_number)}</strong> created for <strong>₹${Number(data.amount).toLocaleString('en-IN')}</strong>. <button type="button" class="btn btn-primary btn-small" id="payNowBtn" style="margin-left:8px">Pay Securely</button>`; document.getElementById('payNowBtn').onclick=()=>startRazorpay(data);} 
  }
  async function startRazorpay(data){
    if(!window.Razorpay){
      await new Promise((resolve,reject)=>{const sc=document.createElement('script');sc.src='https://checkout.razorpay.com/v1/checkout.js';sc.onload=resolve;sc.onerror=reject;document.head.appendChild(sc);});
    }
    if(!window.Razorpay){status('Payment checkout could not be loaded. Please try again.');return;}
    const options={key:data.key_id,amount:Math.round(Number(data.amount)*100),currency:data.currency,name:'Instant Legal Services',description:data.service_name||'ILS Legal Service',order_id:data.razorpay_order_id,handler:async response=>{
      status('Verifying payment…');
      const sb=window.ILS?.ready?.(); if(!sb){status('Service connection unavailable.');return;}
      const {data:verified,error}=await sb.functions.invoke('razorpay-payments',{body:{action:'verify',order_id:data.order_id,razorpay_payment_id:response.razorpay_payment_id,razorpay_signature:response.razorpay_signature}});
      if(error||!verified?.ok){status(error?.message||verified?.error||'Payment verification failed. Please do not pay again until the order status is checked.');return;}
      const box=document.getElementById('toolStatus'); if(box){box.className='status ok';box.innerHTML=`Payment verified successfully. Order <strong>${esc(verified.order_number||data.order_number)}</strong> is now paid.`;}
    },modal:{ondismiss:()=>status('Payment window closed. Your order remains pending.')},theme:{color:'#0b5cff'}};
    const rzp=new Razorpay(options); rzp.on('payment.failed',r=>status(r?.error?.description||'Payment failed. Your order remains pending.')); rzp.open();
  }
  function paidCTA(slug, details={}){
    const service=serviceMap[slug];
    if(!service)return '';
    return `<div class="actions" style="margin-top:14px"><button class="btn btn-primary tool-buy" data-service="${esc(slug)}">Get Professional Service</button></div>`;
  }

  function shell(title, description, body, sourceKey){
    workspace().innerHTML = `<div class="section-head"><div><h2>${esc(title)}</h2><p>${esc(description)}</p></div><button class="btn btn-ghost" id="toolBack">Back to Tools</button></div>${body}${sourceKey && sources[sourceKey] ? `<div class="notice" style="margin-top:14px;font-size:.9rem"><strong>Source / legal basis:</strong> ${sources[sourceKey]}</div>` : ''}<div id="toolStatus" class="status"></div>`;
    document.getElementById('toolBack').onclick = () => { workspace().innerHTML='<div class="empty">Select a tool above to begin.</div>'; window.scrollTo({top:document.querySelector('.section').offsetTop-20,behavior:'smooth'}); };
  }
  function status(message, ok=false){ const el=document.getElementById('toolStatus'); if(!el)return; el.textContent=message; el.className='status '+(ok?'ok':'err'); }
  async function logRun(slug,input,result){
    try{ const sb=window.ILS?.ready?.(); if(!sb)return; const {data:{user}}=await sb.auth.getUser(); if(!user)return; const {data:tool}=await sb.from('tool_catalog').select('id').eq('slug',slug).eq('is_active',true).maybeSingle(); if(!tool)return; await sb.from('tool_runs').insert({tool_id:tool.id,user_id:user.id,input_data:input,result_data:result}); }catch(e){ console.debug('Tool history was not saved.',e); }
  }
  const dateObj = s => new Date(`${s}T12:00:00`);
  const fmt = d => d.toLocaleDateString('en-IN',{day:'2-digit',month:'long',year:'numeric'});
  function addDays(date, days){ const d=new Date(date); d.setDate(d.getDate()+days); return d; }
  function addMonths(date, months){ const d=new Date(date); const day=d.getDate(); d.setDate(1); d.setMonth(d.getMonth()+months); const last=new Date(d.getFullYear(),d.getMonth()+1,0).getDate(); d.setDate(Math.min(day,last)); return d; }
  function deadline(){
    const [t,d]=toolCopy['legal-deadline-calculator']; shell(t,d,`<div class="form-row"><div><label for="dlDate">Event date</label><input id="dlDate" type="date"></div><div><label for="dlDays">Calendar days</label><input id="dlDays" type="number" min="0" step="1" placeholder="e.g. 30"></div></div><div class="actions" style="margin-top:14px"><button class="btn btn-primary" id="dlCalc">Calculate Date</button></div><div id="dlResult" class="notice" style="margin-top:14px;display:none"></div>`);
    document.getElementById('dlCalc').onclick=async()=>{ const date=document.getElementById('dlDate').value,days=Number(document.getElementById('dlDays').value); if(!date||!Number.isInteger(days)||days<0){status('Enter a valid event date and a whole number of days.');return;} const result=fmt(addDays(dateObj(date),days)); const box=document.getElementById('dlResult');box.style.display='block';box.innerHTML=`<strong>Planned date: ${esc(result)}</strong><br><span>This adds ${days} calendar day${days===1?'':'s'}. Statutory exclusions, holidays, filing rules and jurisdiction-specific computation are not automatically applied.</span>${paidCTA('legal-deadline-calculator',{date,days})}`;status('Calculation completed.',true);await logRun('legal-deadline-calculator',{date,days},{planned_date:result}); };
  }
  function interest(){
    const [t,d]=toolCopy['interest-calculator']; shell(t,d,`<div class="form-row"><div><label for="principal">Principal (₹)</label><input id="principal" type="number" min="0" step="0.01"></div><div><label for="rate">Annual rate (%)</label><input id="rate" type="number" min="0" step="0.01"></div></div><div><label for="years">Time (years)</label><input id="years" type="number" min="0" step="0.01"></div><div class="actions" style="margin-top:14px"><button class="btn btn-primary" id="intCalc">Calculate Interest</button></div><div id="intResult" class="notice" style="margin-top:14px;display:none"></div>`);
    document.getElementById('intCalc').onclick=async()=>{const p=Number(principal.value),r=Number(rate.value),y=Number(years.value);if(![p,r,y].every(Number.isFinite)||p<0||r<0||y<0){status('Enter valid non-negative numbers.');return;}const interest=p*r*y/100,total=p+interest,money=n=>n.toLocaleString('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:2});intResult.style.display='block';intResult.innerHTML=`<strong>Simple interest: ${money(interest)}</strong><br>Total: ${money(total)}<br><small>This is simple interest only; agreements, statutes and court awards may use different rules.</small>${paidCTA('interest-calculator',{principal:p,rate:r,years:y})}`;status('Calculation completed.',true);await logRun('interest-calculator',{principal:p,rate:r,years:y},{interest,total});};
  }
  function timeline(){
    const [t,d]=toolCopy['case-timeline'];shell(t,d,`<div id="tlRows"></div><div class="actions" style="margin-top:12px"><button class="btn btn-ghost" id="tlAdd">+ Add Event</button><button class="btn btn-primary" id="tlMake">Generate Timeline</button></div><div id="tlResult" class="notice" style="margin-top:14px;display:none"></div>`);const rows=document.getElementById('tlRows');let n=0;const add=()=>{n++;const div=document.createElement('div');div.className='form-row';div.style.marginBottom='9px';div.innerHTML=`<div><label>Event ${n} date</label><input class="tl-date" type="date"></div><div><label>Event ${n} description</label><input class="tl-desc" maxlength="200" placeholder="e.g. FIR filed"></div>`;rows.appendChild(div)};add();add();tlAdd.onclick=add;tlMake.onclick=async()=>{const data=[...rows.querySelectorAll('.form-row')].map(r=>({date:r.querySelector('.tl-date').value,description:r.querySelector('.tl-desc').value.trim()})).filter(x=>x.date&&x.description).sort((a,b)=>a.date.localeCompare(b.date));if(!data.length){status('Add at least one dated event.');return;}tlResult.style.display='block';tlResult.innerHTML=data.map((x,i)=>`<div style="padding:7px 0;border-bottom:1px solid var(--line)"><strong>${i+1}. ${esc(new Date(x.date+'T12:00:00').toLocaleDateString('en-IN'))}</strong> — ${esc(x.description)}</div>`).join('');status('Timeline generated.',true);await logRun('case-timeline',{events:data},{events:data});};
  }
  function checklist(){const [t,d]=toolCopy['case-checklist'];shell(t,d,`<label for="clMatter">Matter / case type</label><input id="clMatter" maxlength="120" placeholder="e.g. Property dispute"><label for="clItems">Checklist items (one per line)</label><textarea id="clItems" placeholder="Agreement / deed\nIdentity documents\nPrevious orders"></textarea><div class="actions" style="margin-top:12px"><button class="btn btn-primary" id="clMake">Create Checklist</button></div><div id="clResult" class="notice" style="margin-top:14px;display:none"></div>`);clMake.onclick=async()=>{const matter=clMatter.value.trim(),items=clItems.value.split(/\n+/).map(x=>x.trim()).filter(Boolean);if(!matter||!items.length){status('Enter a matter type and at least one checklist item.');return;}clResult.style.display='block';clResult.innerHTML=`<strong>${esc(matter)}</strong><ul>${items.map(x=>`<li>${esc(x)}</li>`).join('')}</ul>`;status('Checklist created.',true);await logRun('case-checklist',{matter,items},{matter,items});};}
  function cheque(){const [t,d]=toolCopy['cheque-bounce-timeline'];shell(t,d,`<div class="form-row"><div><label for="cbDish">Bank dishonour / return information date</label><input id="cbDish" type="date"></div><div><label for="cbNotice">Notice date (optional)</label><input id="cbNotice" type="date"></div></div><div class="actions" style="margin-top:14px"><button class="btn btn-primary" id="cbCalc">Build Timeline</button></div><div id="cbResult" class="notice" style="margin-top:14px;display:none"></div>`,'cheque-bounce-timeline');cbCalc.onclick=async()=>{if(!cbDish.value){status('Enter the bank dishonour / return information date.');return;}const dis=dateObj(cbDish.value),noticeDate=cbNotice.value?dateObj(cbNotice.value):addDays(dis,30),payment=addDays(noticeDate,15),cause=addMonths(payment,1);cbResult.style.display='block';cbResult.innerHTML=`<strong>Indicative timeline</strong><ul><li>30-day notice window: <strong>${esc(fmt(addDays(dis,30)))}</strong></li><li>Notice date used: <strong>${esc(fmt(noticeDate))}</strong></li><li>15-day payment period ends: <strong>${esc(fmt(payment))}</strong></li><li>Indicative complaint timing: <strong>${esc(fmt(cause))}</strong></li></ul><small>Section 138/142 computation depends on valid service, cause of action, jurisdiction and other facts. This is not a filing deadline determination.</small>`;status('Timeline prepared.',true);await logRun('cheque-bounce-timeline',{dishonour_date:cbDish.value,notice_date:cbNotice.value||null},{notice_window_end:fmt(addDays(dis,30)),payment_period_end:fmt(payment),indicative_complaint_date:fmt(cause)});};}
  function limitation(){const [t,d]=toolCopy['limitation-calculator'];shell(t,d,`<div class="form-row"><div><label for="limStart">Starting date</label><input id="limStart" type="date"></div><div><label for="limPeriod">Period</label><input id="limPeriod" type="number" min="0" step="1" placeholder="e.g. 3"></div></div><div><label for="limUnit">Unit</label><select id="limUnit"><option value="years">Years</option><option value="months">Months</option><option value="days">Days</option></select></div><div class="actions" style="margin-top:14px"><button class="btn btn-primary" id="limCalc">Calculate Planning Date</button></div><div id="limResult" class="notice" style="margin-top:14px;display:none"></div>`,'limitation-calculator');limCalc.onclick=async()=>{const p=Number(limPeriod.value);if(!limStart.value||!Number.isInteger(p)||p<0){status('Enter a valid date and whole-number period.');return;}const d0=dateObj(limStart.value),unit=limUnit.value,res=unit==='years'?addMonths(d0,p*12):unit==='months'?addMonths(d0,p):addDays(d0,p);limResult.style.display='block';limResult.innerHTML=`<strong>Planning date: ${esc(fmt(res))}</strong><br><small>This simply applies the period you entered. The Limitation Act and procedural rules can involve different starting points, exclusions, acknowledgements, extensions/condonation and other facts.</small>`;status('Planning calculation completed.',true);await logRun('limitation-calculator',{start:limStart.value,period:p,unit},{planning_date:fmt(res)});};}
  function rateEstimator(slug,title,desc,rateLabel,disclaimer){shell(title,desc,`<div class="form-row"><div><label for="estValue">Base amount / value (₹)</label><input id="estValue" type="number" min="0" step="0.01"></div><div><label for="estRate">Rate (%)</label><input id="estRate" type="number" min="0" step="0.01"></div></div><div><label for="estFixed">Fixed fee (₹), if any</label><input id="estFixed" type="number" min="0" step="0.01" value="0"></div><div class="actions" style="margin-top:14px"><button class="btn btn-primary" id="estCalc">Estimate</button></div><div id="estResult" class="notice" style="margin-top:14px;display:none"></div>`,slug);estCalc.onclick=async()=>{const v=Number(estValue.value),r=Number(estRate.value),f=Number(estFixed.value);if(![v,r,f].every(Number.isFinite)||v<0||r<0||f<0){status('Enter valid non-negative numbers.');return;}const fee=v*r/100+f;estResult.style.display='block';estResult.innerHTML=`<strong>Estimated amount: ${fee.toLocaleString('en-IN',{style:'currency',currency:'INR'})}</strong><br><small>${esc(disclaimer)}</small>`;status('Estimate completed.',true);await logRun(slug,{base_value:v,rate:r,fixed_fee:f},{estimated_fee:fee});};}
  function maintenance(){const [t,d]=toolCopy['maintenance-estimator'];shell(t,d,`<div class="form-row"><div><label for="mIncome">Applicant monthly net income (₹)</label><input id="mIncome" type="number" min="0"></div><div><label for="mOther">Other monthly income/resources (₹)</label><input id="mOther" type="number" min="0" value="0"></div></div><div class="form-row"><div><label for="mDepend">Number of dependants</label><input id="mDepend" type="number" min="1" step="1" value="1"></div><div><label for="mNeed">Monthly essential household needs (₹)</label><input id="mNeed" type="number" min="0" value="0"></div></div><div class="actions" style="margin-top:14px"><button class="btn btn-primary" id="mCalc">Show Budget Snapshot</button></div><div id="mResult" class="notice" style="margin-top:14px;display:none"></div>`,'maintenance-estimator');mCalc.onclick=async()=>{const i=Number(mIncome.value),o=Number(mOther.value),dep=Number(mDepend.value),need=Number(mNeed.value);if(![i,o,dep,need].every(Number.isFinite)||i<0||o<0||dep<1||need<0){status('Enter valid values.');return;}const available=i+o-need,per=available>0?available/dep:0;mResult.style.display='block';mResult.innerHTML=`<strong>Available monthly amount after stated needs: ₹${Math.max(0,available).toLocaleString('en-IN')}</strong><br>Indicative amount per dependant: ₹${per.toLocaleString('en-IN',{maximumFractionDigits:2})}<br><small>This is only a budgeting snapshot. Courts consider facts, income, needs, status, liabilities and the applicable law; there is no universal percentage formula.</small>`;status('Budget snapshot completed.',true);await logRun('maintenance-estimator',{income:i,other_income:o,dependants:dep,needs:need},{available,per_dependant:per});};}
  function diagnostic(){const [t,d]=toolCopy['legal-problem-diagnostic'];shell(t,d,`<label for="diagCategory">Problem category</label><select id="diagCategory"><option value="">Select</option><option>Money / Recovery</option><option>Cheque Bounce</option><option>Property</option><option>Criminal / FIR</option><option>Matrimonial / Family</option><option>Consumer</option><option>Labour / Employment</option><option>Cyber Fraud</option><option>Contract / Business</option><option>Other</option></select><label for="diagState">State / jurisdiction</label><input id="diagState" maxlength="80"><label for="diagFacts">Brief facts</label><textarea id="diagFacts" maxlength="3000" placeholder="What happened, when, and what outcome do you need?"></textarea><div class="actions" style="margin-top:14px"><button class="btn btn-primary" id="diagMake">Create Diagnostic Summary</button></div><div id="diagResult" class="notice" style="margin-top:14px;display:none"></div>`);diagMake.onclick=async()=>{const c=diagCategory.value,s=diagState.value.trim(),f=diagFacts.value.trim();if(!c||!s||f.length<10){status('Select a category, enter jurisdiction and provide at least a short factual summary.');return;}const routes={"Money / Recovery":"Recovery / civil or commercial remedy review","Cheque Bounce":"NI Act Section 138 timeline and notice review","Property":"Title / possession / document due-diligence review","Criminal / FIR":"FIR, arrest, bail and criminal-procedure review","Matrimonial / Family":"Family-law remedy and documentation review","Consumer":"Consumer-law forum and limitation review","Labour / Employment":"Employment / labour-law remedy review","Cyber Fraud":"Bank/cyber reporting and legal-assistance review","Contract / Business":"Contract, notice and recovery/commercial review","Other":"Issue-specific professional review"};diagResult.style.display='block';diagResult.innerHTML=`<strong>Possible route for review:</strong> ${esc(routes[c])}<br><strong>Jurisdiction:</strong> ${esc(s)}<br><strong>Next step:</strong> organise documents, dates and evidence before professional assessment.`;status('Diagnostic summary created.',true);await logRun('legal-problem-diagnostic',{category:c,state:s,facts:f},{possible_route:routes[c]});};}
  function casePrep(){const [t,d]=toolCopy['case-preparation-tool'];shell(t,d,`<label for="cpTitle">Matter title</label><input id="cpTitle" maxlength="150"><label for="cpFacts">Key facts / chronology</label><textarea id="cpFacts" maxlength="5000"></textarea><label for="cpIssues">Issues / questions to resolve</label><textarea id="cpIssues" maxlength="3000"></textarea><label for="cpDocs">Documents available (one per line)</label><textarea id="cpDocs" maxlength="3000"></textarea><div class="actions" style="margin-top:14px"><button class="btn btn-primary" id="cpMake">Prepare Case Pack</button></div><div id="cpResult" class="notice" style="margin-top:14px;display:none"></div>`);cpMake.onclick=async()=>{const title=cpTitle.value.trim(),facts=cpFacts.value.trim(),issues=cpIssues.value.trim(),docs=cpDocs.value.split(/\n+/).map(x=>x.trim()).filter(Boolean);if(!title||facts.length<10){status('Enter a matter title and meaningful facts.');return;}cpResult.style.display='block';cpResult.innerHTML=`<strong>${esc(title)}</strong><h4>Facts</h4><p>${esc(facts).replace(/\n/g,'<br>')}</p><h4>Issues</h4><p>${esc(issues||'Not provided').replace(/\n/g,'<br>')}</p><h4>Documents</h4><ul>${(docs.length?docs:['No documents listed']).map(x=>`<li>${esc(x)}</li>`).join('')}</ul><small>Preparation aid only; it is not a legal opinion or substitute for professional review.</small>${paidCTA('case-preparation-tool',{title,facts,issues,documents:docs})}`;status('Case preparation pack created.',true);await logRun('case-preparation-tool',{title,facts,issues,documents:docs},{title,facts,issues,documents:docs});};}
  function propertyChecklist(){const [t,d]=toolCopy['property-document-checklist'];const common=['Title / sale deed','Previous chain documents','Mutation / revenue records','Latest property tax receipts','Encumbrance / search report where applicable','Approved map / sanctioned plan where applicable','Identity and authority documents of parties','Possession / handover evidence','Existing loan / mortgage release documents where applicable','Litigation / notice information if any'];shell(t,d,`<label for="pcType">Transaction / property type</label><select id="pcType"><option>Sale / Purchase</option><option>Gift</option><option>Inheritance</option><option>Mortgage / Loan</option><option>Commercial Property</option><option>Other</option></select><p>Select the documents you have:</p><div id="pcItems">${common.map((x,i)=>`<label style="display:block;margin:8px 0"><input type="checkbox" value="${esc(x)}"> ${esc(x)}</label>`).join('')}</div><div class="actions" style="margin-top:14px"><button class="btn btn-primary" id="pcMake">Create Checklist</button></div><div id="pcResult" class="notice" style="margin-top:14px;display:none"></div>`,'property-document-checklist');pcMake.onclick=async()=>{const type=pcType.value,have=[...pcItems.querySelectorAll('input:checked')].map(x=>x.value),missing=common.filter(x=>!have.includes(x));pcResult.style.display='block';pcResult.innerHTML=`<strong>${esc(type)}</strong><h4>Documents to organise / verify</h4><ul>${missing.map(x=>`<li>${esc(x)}</li>`).join('')||'<li>All common checklist items selected.</li>'}</ul><small>This is a general checklist, not a title-clearance opinion. Requirements vary by state, property and transaction.</small>`;status('Property checklist created.',true);await logRun('property-document-checklist',{type,available:have},{missing});};}
  function open(slug){const map={'legal-deadline-calculator':deadline,'interest-calculator':interest,'case-timeline':timeline,'case-checklist':checklist,'cheque-bounce-timeline':cheque,'limitation-calculator':limitation,'court-fee-calculator':()=>rateEstimator('court-fee-calculator','Court Fee Estimator',toolCopy['court-fee-calculator'][1],'Rate','This estimate uses only the rate and fixed amount you supplied. Actual court fee may be governed by state amendments, schedules, court rules and case valuation.'),'stamp-duty-calculator':()=>rateEstimator('stamp-duty-calculator','Stamp Duty Estimator',toolCopy['stamp-duty-calculator'][1],'Rate','This estimate uses only the rate and fixed amount you supplied. Actual stamp duty depends on the applicable state law, instrument, consideration, market value and other facts.'),'maintenance-estimator':maintenance,'legal-problem-diagnostic':diagnostic,'case-preparation-tool':casePrep,'property-document-checklist':propertyChecklist};if(map[slug])map[slug]();}
  document.addEventListener('DOMContentLoaded',()=>document.querySelectorAll('.tool-open').forEach(b=>b.addEventListener('click',()=>open(b.dataset.tool))));
  document.addEventListener('click',e=>{const b=e.target.closest('.tool-buy');if(!b)return;createOrder(b.dataset.service,{});});
  return {open,createOrder};
})();
