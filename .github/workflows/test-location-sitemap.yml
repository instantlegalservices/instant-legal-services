name: Test Location Sitemap

on:
  workflow_dispatch:

  push:
    branches:
      - v9-final-test
    paths:
      - "scripts/location-sitemap.js"
      - "scripts/test-location-sitemap.js"
      - ".github/workflows/test-location-sitemap.yml"

permissions:
  contents: read

jobs:
  test-location-sitemap:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Run location sitemap tests
        run: node scripts/test-location-sitemap.js
