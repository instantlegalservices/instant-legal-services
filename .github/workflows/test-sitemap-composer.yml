name: Test Sitemap Composer

on:
  workflow_dispatch:
  push:
    branches:
      - v9-final-test
    paths:
      - "scripts/sitemap-composer.js"
      - "scripts/test-sitemap-composer.js"
      - "scripts/location-registry-feed.js"
      - "scripts/location-sitemap.js"
      - ".github/workflows/test-sitemap-composer.yml"

permissions:
  contents: read

jobs:
  test-sitemap-composer:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Verify Node.js version
        run: node --version

      - name: Run adversarial composer tests
        run: node scripts/test-sitemap-composer.js
